decoration {
    rounding = 12
    active_opacity = 0.98
    inactive_opacity = 1.0
    fullscreen_opacity = 1.0

    blur {
        enabled = true
        size = 8
        passes = 1
        new_optimizations = true
        xray = true
        ignore_opacity = true
    }

    # Remove all shadow-related options for now
}
